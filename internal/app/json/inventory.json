{
  "docker_counts": {
    "containers": 3,
    "images": 4,
    "networks": 2,
    "volumes": 1
  },
  "environment": {
    "confidence": "heuristic",
    "evidence": [
      "The visible API endpoint is an internal-style address without strong managed-cluster branding."
    ],
    "summary": "The visible API endpoint looks private or lab-shaped rather than strongly managed-service-branded.",
    "type": "self-managed-like"
  },
  "exposure_footprint": {
    "host_exposure_pods": 2,
    "ingresses": 1,
    "load_balancers": 1,
    "node_ports": 1,
    "public_paths": 3,
    "summary": "3 public-looking exposure paths are visible, so `exposure` should likely be the next stop."
  },
  "identity_footprint": {
    "cluster_wide_role_grants": 1,
    "high_impact_service_accounts": 2,
    "role_grants": 3,
    "service_accounts": 3,
    "summary": "3 service accounts, 3 visible role grants, and 2 higher-impact service-account path(s) are visible."
  },
  "issues": [],
  "kubernetes_counts": {
    "configmaps": 4,
    "daemonsets": 1,
    "deployments": 2,
    "ingresses": 1,
    "namespaces": 4,
    "nodes": 3,
    "pods": 5,
    "secrets": 6,
    "services": 3,
    "statefulsets": 1
  },
  "metadata": {
    "cluster_name": null,
    "command": "inventory",
    "context_name": "lab-cluster",
    "docker_context": "default",
    "generated_at": "2026-04-07T23:14:44Z",
    "namespace": "default",
    "schema_version": "1.0.0"
  },
  "next_commands": [
    {
      "command": "exposure",
      "why": "3 public-looking exposure path(s) are visible, so the cluster edge deserves review first."
    },
    {
      "command": "workloads",
      "why": "2 visible workload(s) already carry privileged or host-touching clues."
    },
    {
      "command": "service-accounts",
      "why": "Visible service-account use and RBAC grants suggest identity paths are already worth triaging."
    },
    {
      "command": "rbac",
      "why": "Visible role grants are already on screen, so `rbac` can ground the assignment evidence directly."
    }
  ],
  "risky_workload_footprint": {
    "docker_socket_workloads": 1,
    "host_namespace_workloads": 1,
    "host_touching_workloads": 2,
    "privileged_workloads": 1,
    "summary": "1 privileged and 2 host-touching workload clue(s) are visible, so `workloads` should help quickly."
  },
  "visibility": {
    "assessment": "broad",
    "scope": "broad cluster slice",
    "summary": "Current scope shows 4 namespaces, 3 nodes, and 9 visible workload objects, so this reads like a broad cluster orientation pass."
  }
}